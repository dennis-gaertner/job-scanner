/*
Job Scanner: see README.md for the current architecture, file map,
entry points, verification and installation instructions.

Production entry point: scanAllJobSources()
Persistent stores: Jobs_All and Jobs_User.
Shared derived view: buildDerivedJobViews_().
The full run includes notifications and optional external archiving.
*/
